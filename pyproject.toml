[project]
name = "arthur-mask"
version = "0.1.0"
description = "Arthur Mask: Türk hukuk belgeleri (UDF/DOCX/PDF) için yerel takma adlandırma"
requires-python = ">=3.10"
license = { text = "LicenseRef-ArthurLegal-Proprietary-Non-Commercial" }
dependencies = [
    "presidio-analyzer>=2.2.364",
    "spacy>=3.7",
    "pyyaml>=6",
    "cryptography>=42",
    "pypdf>=5",
    "mcp>=2.2",
]

[project.optional-dependencies]
test = ["pytest>=8", "python-docx>=1.1", "reportlab>=4"]
semantik = ["gliner>=0.2.29", "presidio-analyzer[gliner]>=2.2.364", "protobuf", "sentencepiece"]

[project.scripts]
arthur-mask = "arthur_mask.cli:main"
arthur-mask-kopru = "arthur_mask.kopru:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["arthur_mask*"]

[tool.setuptools.package-data]
arthur_mask = ["veri/*.txt", "arayuz/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning", "ignore::UserWarning"]
